<div class="topDeck">
  <h3><%= @deck.title %></h3>
</div>
  <div class="deckBack">
    <%= gmaps4rails(@json) %>
    <%= image_tag @deck.image%>
    <div class="tripInfoBack">
      <h3><%= @deck.title%></h3>
    </div>
  </div>
<div class="deckComs">
  <div class=""><%= render partial: 'likes/like', locals: { likeable: @deck } %></div>
  <%= render partial: 'decks/keep', locals: { deck: @deck } %>
  <%= render partial: 'decks/social', locals: { deck: @deck } %>
  <% if can? :manage, @deck %>
    <div class="memberOnly">
      <span><%= link_to "Edit", edit_deck_path(@deck), class: "keep" %></span>
      <span> <%= link_to "Delete", @deck, method: :delete, class: "keep", confirm: 'Are you sure you want to delete this deck?' %></span>
    </div>
  <% end %>
</div>
<div class="deckCards">
  <div class="topDeck">
    <%= render @cards %>
    <div class="apple_pagination">
      <%= will_paginate @cards %>
    </div>
  </div>
</div>



