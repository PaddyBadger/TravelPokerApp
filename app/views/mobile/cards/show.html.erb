<div class="allMobile">
    <div class="tripBack">
      
       <%= gmaps4rails(@json) %>
       <%= image_tag @card.image%>
      <div class="tripInfoBack">
        <h3><%= @card.title%></h3> 
      </div>

     <!-- <div class="deckComs">
        <div class=""><%= render partial: 'likes/like', locals: { likeable: @card } %></div>
        <%= render partial: 'cards/social', locals: { card: @card } %>
        <%= render partial: 'cards/keep', locals: { card: @card } %>
       
        <% if can? :manage, @card %>
        <div class="memberOnly">
          <span><%= link_to "Edit", edit_card_path(@card), class: "dk_label editDelete" %></span>
          <span> <%= link_to "Delete", @card, method: :delete, class: "dk_label editDelete", confirm: 'Are you sure you want to delete this card?' %></span>
        </div>
        <% end %>
      </div>
     -->
      <div class="cardBackMobile">
        <h4><%= @card.location%></h4>  
      
        <p><%= @card.description%></p>

        <div class="mapDirections">
          <a href="https://maps.google.com/maps?saddr=Current%20Location&daddr=<% @card.location %>"><div class="directions"><h4>Directions</h4><img src='/assets/directions.png'></div></a>
          <div class="requestMap"><h4>Map</h4><img src='/assets/bluePin.png'></div>
        </div>



         <div class="advert">
            <script async src="//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
            <!-- tp backs -->
            <ins class="adsbygoogle"
                 style="display:inline-block;width:728px;height:90px"
                 data-ad-client="ca-pub-1244420710031131"
                 data-ad-slot="9375366351"></ins>
            <script>
            (adsbygoogle = window.adsbygoogle || []).push({});
            </script>
          </div>  
        <!-- <p><%= link_to @card.url%></p>
        <h3>Decks this Card is Part of</h3>
        <% @card.decks.each do |deck| %>
          <%= link_to deck.title, deck %><br>
        <% end %>

        <div class="categoryMobile"><h3>Category</h3><%=image_tag @card.category.image, title: @card.category.title %></div>
        <div class="seasonMobile"><h3>Season</h3><%=image_tag @card.season.image %></div>

        <div class="comments">  
          <h3 class='js-comments-count'><%= @comments.count %> Additional Thoughts</h3>
            <div class="js-comments"> 
              <%= render partial: "comments/comment", collection: @comments, locals: { card: @cards} %>
            </div>
             <%= render partial: "comments/form", locals:{ card: @card, comment: Comment.new } %>
        </div> -->
      </div>
    </div>
</div>
